name: Release (Portable)

# 此工作流已由 build-release.yml 替代。
# 新代码使用 Nuitka 打包，不再需要单独的便携包构建。
# 触发: 推送 p* tag → 见 .github/workflows/build-release.yml

on:
  push:
    tags: ["p*"]

jobs:
  delegate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Portable 构建已委托给 build-release.yml 工作流"
