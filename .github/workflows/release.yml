name: Release

# 此工作流已由 build-release.yml 替代。
# 新代码使用 Nuitka 打包（scripts/build_nuitka.py），不再使用 PyInstaller。
# 触发: 推送 v* tag → 见 .github/workflows/build-release.yml

on:
  push:
    tags: ["v*"]

jobs:
  delegate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release 构建已委托给 build-release.yml 工作流"
