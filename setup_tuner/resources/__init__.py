"""随包分发的资源（模型产物等）。

为什么放在包内而不是仓库根的 data/models/：
``pip install`` 之后只有包目录会被安装，仓库根的 data/ 不存在 —— 模型放在
仓库根会导致"从 GitHub 装好却找不到模型、静默降级为纯规则"。
放在包内即随包分发，安装后开箱可用（引擎优先读工作目录 data/models/，
其次读包内资源，见 engine/model_io.py）。
"""
